import React from "react";
import { SafeAreaView } from "react-native";
import Contador from "./components/botoes/contador";

export default () => {
    return (
        <SafeAreaView>
            <Contador />
        </SafeAreaView>
    );
}